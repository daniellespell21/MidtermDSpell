
	function calcAvgSquare(num1, num2, num3) {
	  let avgSquare=((num1+num2+num3)/3)*(num1*num1)
	  return avgSquare}
	function randomMultiply(ranNum){
	  let randomizedNumber=ranNum*(Math.floor(Math.random()*10))
	  return randomizedNumber
	  }
	let question=prompt('Would you like to do an average calculation or a random multiplication?(Enter average or random)')
	
	if (question=='average'){
	  num4=Number(prompt('Enter a number'))
	
	  num5=Number(prompt('Enter a number'))
	
	  num6=Number(prompt('Enter a number'))
	
	  alert(`The answer is ${calcAvgSquare(num4,num5,num6)}`)
	} else if (question=='random') {
	  num7=Number(prompt('Enter a number to be multiplied times a random number between 1-10'))
	  
	  alert(`The result of multiplying the random number with ${num7} is ${randomMultiply(num7)}.`)
	} else {alert('Type either random or average')}
	  
